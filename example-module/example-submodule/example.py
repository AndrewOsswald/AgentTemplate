#!/usr/bin/env python3
# Example submodule stub. Template only; example-module/ is deleted during setup.

def main() -> None:
    print("Example submodule — nesting uses the same format as top-level modules.")

if __name__ == "__main__":
    main()
