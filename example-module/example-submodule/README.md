# Module: Example submodule

A nested example: same layout as a top-level module (README, context/, code) but inside **example-module/**. Shows that **nesting is part of the format**—modules can live at repo root or inside another module. **Template only;** deleted when you run setup (the whole **example-module/** tree is removed).

## Project

Placeholder submodule under example-module. Use it as a reference for nested structure; see **agent/new.md** for creating real submodules.

## Best practices

- Same format at every level: README, **context/current-state.md**, **context/wip-*.md**, code. Nested or not.

## Overview

- **README.md** (this file) — For humans.
- **context/current-state.md** — For agents. Module entry, rules, code, working/not/next.
- **context/wip-example.md** — Example WIP doc.

## How to run

Nothing to run. Reference only.

## Deeper docs (in context/)

- **context/current-state.md** — Current state (for agents).
- **context/wip-example.md** — Example WIP format.

## Related WIP doc(s)

- **context/wip-example.md** — Example WIP (template only).
